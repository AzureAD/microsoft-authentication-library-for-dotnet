steps:

# Install packages and run Python mTLS PoP E2E test (Windows)
- script: |
    python -m pip install --upgrade pip
    python -m pip install msal-msiv2==1.35.0rc3 requests cryptography
    python build\test_mtls_pop_imdsv2.py
  displayName: 'Run Managed Identity E2E Tests – IMDSv2 Python'
