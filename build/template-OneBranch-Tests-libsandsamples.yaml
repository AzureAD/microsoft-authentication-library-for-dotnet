# template-OneBranch-Tests-libsandsamples.yaml
# Performs Nuget Restore and Build of LibsAndSamples.sln based on BuildPlatform and BuildConfiguration

parameters:
  BuildPlatform: 'any cpu'
  BuildConfiguration: 'release'
  MsalClientSemVer: '4.0.0-devopsbuild'
  MsalSourceDir: '' #Default MSAL Location

steps:

- template: template-OneBranch-CI-libsandsamples.yaml
  parameters:
    Solution: '${{ parameters.MsalSourceDir }}LibsAndSamples.sln'
    MsalSourceDir: '${{ parameters.MsalSourceDir }}'
    PipelineType: $(PipelineType)

  # Install ChromeDriver for Selenium-based integration tests.
  # The 1ES Windows 2022 Starter Image includes Google Chrome but not ChromeDriver.
  # SeleniumExtensions.cs resolves chromedriver via the CHROMEWEBDRIVER env var, so we set it here.
  # We match the ChromeDriver version to the installed Chrome version via the Chrome for Testing API.
- task: PowerShell@2
  displayName: 'Install ChromeDriver'
  inputs:
    targetType: 'inline'
    script: |
      # Get the installed Chrome version
      $chromePath = "C:\Program Files\Google\Chrome\Application\chrome.exe"
      $chromeVersion = (Get-Item $chromePath).VersionInfo.ProductVersion
      $chromeMajor = $chromeVersion.Split('.')[0]
      Write-Host "Detected Chrome version: $chromeVersion (major: $chromeMajor)"

      # Fetch the matching ChromeDriver version from the Chrome for Testing API
      $apiUrl = "https://googlechromelabs.github.io/chrome-for-testing/LATEST_RELEASE_$chromeMajor"
      $driverVersion = (Invoke-WebRequest -Uri $apiUrl -UseBasicParsing).Content.Trim()
      Write-Host "Downloading ChromeDriver version: $driverVersion"

      # Download and extract ChromeDriver
      $driverDir = "C:\tools\chromedriver"
      $zipPath = "$env:TEMP\chromedriver.zip"
      $downloadUrl = "https://storage.googleapis.com/chrome-for-testing-public/$driverVersion/win64/chromedriver-win64.zip"
      Invoke-WebRequest -Uri $downloadUrl -OutFile $zipPath -UseBasicParsing
      Expand-Archive -Path $zipPath -DestinationPath $driverDir -Force

      # The zip extracts to a chromedriver-win64 subfolder
      $driverBin = Join-Path $driverDir "chromedriver-win64"
      Write-Host "##vso[task.setvariable variable=CHROMEWEBDRIVER]$driverBin"
      Write-Host "ChromeDriver installed to: $driverBin"

  # Run All Desktop Tests
- template: template-run-all-tests.yaml
  parameters:
    BuildConfiguration: '$(BuildConfiguration)'

- task: VSTest@2
  displayName: 'Run cache compat tests'
  condition: and(succeeded(), eq(variables['RunTests'], 'true'))
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: '**\CacheCompat\CommonCache.Test.Unit\bin\**\CommonCache.Test.Unit.dll'
    searchFolder: '$(System.DefaultWorkingDirectory)'
    codeCoverageEnabled: false
    failOnMinTestsNotRun: true
    minimumExpectedTests: '1'
    runInParallel: true
