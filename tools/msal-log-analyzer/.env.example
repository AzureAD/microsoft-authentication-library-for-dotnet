# MSAL Log Analyzer Environment Configuration
# Copy this file to .env and adjust the values as needed.

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=3001

# Allowed CORS origin (use * for development, restrict in production)
CORS_ORIGIN=*

# Node environment (development | production)
NODE_ENV=development

# ─── Database ─────────────────────────────────────────────────────────────────
# Directory for the SQLite database file
DB_DIR=./data

# ─── AI Integration (optional) ────────────────────────────────────────────────
# Provide an Anthropic API key to enable Claude AI-powered log summarization.
# If not set, the built-in regex-based parser is used.
# ANTHROPIC_API_KEY=sk-ant-...
